# Getting Started

Passos rápidos para começar com `checkpix`:

1. Instale dependências (Debian/Ubuntu):

```bash
sudo apt-get update
sudo apt-get install -y curl zenity xdg-utils
```

2. Torne o script executável e rode:

```bash
chmod +x pix.sh
./pix.sh
```
